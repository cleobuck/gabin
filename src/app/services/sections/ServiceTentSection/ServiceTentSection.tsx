import React from 'react';
import styles from './ServiceTentSection.module.scss';

type Props = {};

const ServiceTentSection: React.FC<Props> = ({}) => {
  return (
    <div className={styles.container}>
      ServiceTentSection
    </div>
  );
};

export default ServiceTentSection;
